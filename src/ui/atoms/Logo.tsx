import { Link } from "@/navigation";

export const Logo = () => {
	return (
		<Link href="/" className="z-50 text-center font-secondary ">
			<div className="text-lg font-semibold tracking-wider lg:text-base">Konrad Jankowski</div>
		</Link>
	);
};
